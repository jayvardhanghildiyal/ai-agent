[project]
name = "ai-agent"
version = "0.1.0"
description = "an ai agent that helps with code queries"
readme = "readme.MD"
requires-python = ">=3.12"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
